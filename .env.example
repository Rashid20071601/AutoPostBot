# ==========================================
# 🤖 TELEGRAM BOT SETTINGS
# ==========================================
# Токен вашего Telegram-бота, полученный у @BotFather
BOT_TOKEN=your_telegram_bot_token_here


# ==========================================
# 🧾 LOGGING SETTINGS
# ==========================================
# Возможные уровни: DEBUG | INFO | WARNING | ERROR | CRITICAL
LOG_LEVEL=DEBUG

# Формат вывода логов (использует стиль форматирования "str.format")
# Поддерживаемые параметры: {asctime}, {levelname}, {filename}, {lineno}, {name}, {message}
LOG_FORMAT=[{asctime}] {levelname:8} {filename}:{lineno} - {name} - {message}

# Имя файла, в который записываются логи
LOG_FILENAME=logs.log

# Режим записи:
#   a — добавлять новые записи в конец файла
#   w — перезаписывать файл при каждом запуске
LOG_FILEMODE=w

# Кодировка файла логов
LOG_ENCODING=utf-8


# ==========================================
# 🐘 POSTGRESQL DATABASE SETTINGS
# ==========================================
POSTGRES_DB=autopostbot
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres


# ==========================================
# ⚙️ OPTIONAL SETTINGS
# ==========================================
# Значение True/False. Если True — планировщик активируется при запуске.
ENABLE_SCHEDULER=True

# Если нужно запускать бота в тестовом режиме (без отправки реальных сообщений)
DEBUG_MODE=False
